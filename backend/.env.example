# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# Frontend URL (deploy frontend to Vercel or other platform)
FRONTEND_URL=https://your-frontend-url.vercel.app

# Backend URL (will be ngrok URL when running locally, changes each time)
# Example: https://abc123.ngrok.io
# The start-dev script will automatically update the webhook with this URL
BACKEND_URL=https://your-ngrok-url.ngrok.io

# Database Configuration (for local Docker development)
# These are used by docker-compose.yml
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ofm

# Local PostgREST Configuration (automatically set by docker-compose)
# The backend will use this when SUPABASE_URL is not set
LOCAL_SUPABASE_URL=http://localhost:8000
LOCAL_SUPABASE_ANON_KEY=dev-local-noauth

# Optional: Hosted Supabase (if you prefer using hosted Supabase instead of local Docker)
# Leave these empty to use local Docker database
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Server Configuration
PORT=3000
NODE_ENV=development

